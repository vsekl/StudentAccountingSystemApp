databaseChangeLog:
  - changeSet:
      id: create_groups
      author: Vsevolod
      changes:
        - sql:
            sql: CREATE TABLE groups(
              id SERIAL PRIMARY KEY,
              name VARCHAR(30) NOT NULL UNIQUE
              );